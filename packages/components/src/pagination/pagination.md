


```jsx padded
    import { Pagination } from '@safe/components';
    const onChange = (pageNumber) => {
      console.log('Page: ', pageNumber);
    };
  
  <Pagination defaultCurrent={1} total={500}  showSizeChanger showQuickJumper/>
  
```
